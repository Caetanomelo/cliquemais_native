# Add project-specific ProGuard rules here as R8/shrinking issues surface
# (most Flutter plugin AARs ship their own consumer rules, merged
# automatically — this file is a place to patch anything they miss).
